apiVersion: v1
kind: Service
metadata:
  name: ml-scorer
  namespace: kubehealer
  labels:
    app: ml-scorer
spec:
  selector:
    app: ml-scorer
  ports:
    - name: metrics
      port: 9105
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
